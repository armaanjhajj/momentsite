"""SCENT, learning a coordinate system for smell.

Nothing here has been trained. See ../README.md for why, and for the seam that
lets a trained model replace the site's current embedding without touching the
front end.
"""

__all__ = ["data", "featurize", "model", "train", "embed", "eval_snitz"]
